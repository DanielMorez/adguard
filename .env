# Внутренний порт веб-интерфейса AdGuard Home (только внутри Docker-сети, наружу не пробрасывается)
ADGUARD_WEB_PORT=3000

# Порты на хосте: DNS, HTTP и HTTPS
DNS_PORT=53
HTTP_PORT=80
HTTPS_PORT=443

# Часовой пояс для контейнера AdGuard Home
TZ=Europe/Moscow
